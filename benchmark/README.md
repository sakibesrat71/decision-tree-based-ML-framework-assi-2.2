# Benchmark

Run the benchmark from the repository root:

```bash
python benchmark/stream_benchmark.py
```

It compares a single decision tree and an ensemble under the same streaming
scenario, then prints runtime and cumulative accuracy in CSV-style rows.
