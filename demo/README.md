# Demo

Run the streaming demo from the repository root:

```bash
python demo/stream_demo.py
```

The script trains a single decision tree and an ensemble on the same incoming
chunks. It prints final cumulative accuracy and saves plots in `outputs/`.
